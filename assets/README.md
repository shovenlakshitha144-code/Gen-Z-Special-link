# Assets Directory

This folder holds optional custom media assets for the proposal experience:

- `music.mp3` - Optional background music track (MP3 format). If not present, the built-in Web Audio API romantic generative synth provides ambient romantic melodies and interactive sound effects automatically.
- `photo.jpg` - Optional couple photo for the final keepsake polaroid card. By default, a curated high-resolution romantic image is displayed.
